{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist", ".output", ".nuxt", "build"]
    },
    "lint": {
      "outputs": []
    },
    "test": {
      "outputs": ["coverage"]
    },
    "clean": {
      "cache": false
    }
  }
}
